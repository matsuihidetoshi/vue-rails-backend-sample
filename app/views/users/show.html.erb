<div class="text-center">
  <h1>管理者情報</h1>
</div>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div>管理者名：<%= @user.name %></div>
    <div>メールアドレス：<%= @user.email %></div>
    <%= link_to '編集', edit_user_path(@user), class: 'btn btn-success btn-block' %>
    <%= link_to '削除', @user, method: :delete, data: {confirm: '管理者を削除しますか？'}, class: 'btn btn-danger btn-block' %>
    
    <div data-turbolinks="false" class="return">
      <%= link_to 'トップページへ戻る', root_path, class: 'btn btn-warning btn-block' %>
    </div>
  </div>
</div>